@model IEnumerable<CWE.Models.Notifier>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Notifier_NotificationString)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Notifier_NotificationString)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Notifier_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Notifier_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Notifier_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
